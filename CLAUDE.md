# CLAUDE.md - Weekly Tech Radar Project Guide

## 项目概述

Weekly Tech Radar 是一个赛博朋克风格的技术周刊单页应用模板。采用内容与展示分离的架构，所有内容使用 Markdown 编写，通过动态加载渲染到页面。

**核心特性：**
- 🎨 赛博朋克暗黑风格设计，霓虹发光效果
- 📝 Markdown 内容管理，支持 YAML Frontmatter
- 💻 代码语法高亮（Highlight.js）
- 📱 完全响应式，适配移动端和桌面端
- ⚡ 纯前端实现，无需后端服务器
- 🔥 交互功能：点赞、阅读量统计、侧边栏导航

## 技术栈

### 核心技术
- **HTML5 + CSS3**：页面结构和样式
- **Vanilla JavaScript**：无框架，纯 JS 实现
- **Marked.js v11.1.1**：Markdown 解析
- **js-yaml v4.1.0**：YAML Frontmatter 解析
- **Highlight.js v11.9.0**：代码语法高亮（Tokyo Night Dark 主题）

### 字体
- **Google Fonts**：
  - Inter：主要文本字体
  - JetBrains Mono：等宽字体（代码、技术元素）

## 架构说明

### 文件结构

```
tech_radar_web/
├── index.html                    # 动态加载版本（推荐使用）
├── server.js                     # Node.js 服务器（提供 API 和并发控制）
├── README.md                     # 用户文档
├── CLAUDE.md                     # AI 助手指南（本文件）
├── assets/                       # 静态资源
│   └── images/
│       └── avatars/              # 作者头像
└── content/                      # 内容目录（Markdown 文件）
    ├── config.md                 # 全局配置（站点标题、slogan、footer）
    ├── archive.json              # 往期周刊索引（静态服务器使用）
    ├── authors.md                # 统一作者档案文件（所有作者信息）
    └── vol-XXX/                  # 每期周刊独立目录
        ├── radar.md              # This Week's Radar 内容
        └── contributions/        # 投稿文章目录
            ├── 01-*.md
            ├── 02-*.md
            └── ...
```

### 内容管理架构

1. **双模板设计**：
   - `index.html`：动态版本，从 Markdown 加载内容
   - `weekly-tech-radar.html`：静态版本，内容硬编码在 HTML 中

2. **内容分离原则**：
   - 所有内容存储在 `content/` 目录
   - 每期周刊独立目录（`vol-001/`, `vol-002/`...）
   - 使用 YAML Frontmatter 存储元数据
   - 作者信息统一管理在 `content/authors.md`（单一文件包含所有作者）
   - 全局配置（title, slogan）在 `content/config.md`
   - 每期编辑团队在各期的 `radar.md` 中指定

3. **动态加载流程**：
   ```
   页面加载 → 解析 URL 参数 → 加载对应期刊
   ↓
   加载 radar.md → 渲染 Trending 部分
   ↓
   加载 contributions/*.md → 渲染投稿卡片
   ↓
   加载 archive.json → 渲染侧边栏导航
   ```

## Markdown 格式规范

### Radar 文件 (radar.md)

```markdown
---
vol: "期数（三位数字）"
date: "YYYY.MM.DD"
editors:
  - author_id: "hys"
    role: "Chief Editor"
  - author_id: "dev_ops"
    role: "Technical Reviewer"
---

## Trending

### [徽章分类] 条目标题
详细内容...

### [另一个分类] 另一个标题
详细内容...
```

**说明：**
- `vol` 和 `date` 是必填字段
- `editors` 引用 `content/authors.md` 中定义的作者
- 每个编辑需指定 `author_id` 和 `role`（本期角色：主编/副主编/编辑/审核）
- 全局的 title 和 slogan 在 `content/config.md` 中定义

**徽章样式映射：**
- `[架构决策]` → `.badge.architecture` → 青色
- `[债务预警]` → `.badge.debt` → 橙色
- `[工具推荐]` → `.badge.tool` → 绿色
- `[安全更新]` → `.badge.security` → 粉色

### 投稿文件 (contributions/*.md)

```markdown
---
author_id: "zhang_wei"
title: "文章标题"
description: "简短描述（1-2 句话）"
---

\```语言标识符
代码内容
\```

或者纯文本内容（支持完整 Markdown 语法）
```

**说明：**
- `author_id` 引用 `content/authors.md` 中定义的作者
- 作者的 name, team, avatar, role 等信息从 authors.md 自动加载
- views 和 likes 由服务器动态管理，不需要在文件中指定

### 统一作者文件 (authors.md)

```markdown
---
authors:
  - id: "zhang_wei"
    name: "@zhang_wei"
    team: "Core Platform Team"
    avatar: "/assets/images/avatars/zhang_wei.jpg"
    role: "Senior Developer"

  - id: "hys"
    name: "@hys"
    team: "Engineering Team"
    avatar: "/assets/images/avatars/hys.jpg"
    role: "Tech Lead"
---
```

**说明：**
- 所有作者信息存储在单一文件 `content/authors.md` 中
- `id` 是唯一标识符，用于在其他文件中引用
- `name` 是显示名称，通常以 @ 开头
- `team` 是作者所属团队
- `avatar` 建议使用本地路径
- `role` 是作者的职位/角色（如 Tech Lead, Senior Developer 等）

**作者属性说明：**
| 属性 | 说明 | 示例 |
|------|------|------|
| `id` | 唯一标识符 | "zhang_wei" |
| `name` | 显示名称 | "@zhang_wei" |
| `team` | 所属团队 | "Core Platform Team" |
| `avatar` | 头像路径 | "/assets/images/avatars/zhang_wei.jpg" |
| `role` | 职位/角色 | "Senior Developer" |

**显示位置：**
- **Header**：显示编辑的名字和本期贡献角色（主编/副主编/编辑/审核）
- **开发者空间卡片**：显示作者头像、name、team 和 role 标签

**支持的语言标识符示例：**
- `typescript`, `tsx`
- `javascript`, `jsx`
- `python`, `go`, `rust`, `java`
- `yaml`, `json`, `xml`
- `bash`, `shell`

## 样式系统

### CSS 变量（在 `:root` 中定义）

```css
/* 颜色系统 */
--bg-primary: #0a0a0a;        /* 主背景 */
--bg-secondary: #151515;      /* 次级背景 */
--bg-card: #1a1a1a;           /* 卡片背景 */
--text-primary: #ededed;      /* 主文本 */
--text-secondary: #9ca3af;    /* 次级文本 */

/* 强调色（霓虹色） */
--accent-cyan: #00f3ff;       /* 青色（主强调色）*/
--accent-pink: #ff00ff;       /* 粉色（次强调色）*/
--accent-green: #00ff88;      /* 绿色 */
--accent-orange: #ff6b35;     /* 橙色 */
--accent-purple: #a855f7;     /* 紫色 */

/* 字体 */
--font-main: 'Inter', sans-serif;
--font-mono: 'JetBrains Mono', monospace;

/* 间距 */
--spacing-xs: 0.5rem;
--spacing-sm: 1rem;
--spacing-md: 1.5rem;
--spacing-lg: 2rem;
--spacing-xl: 3rem;
```

### 关键 CSS 类

**布局类：**
- `.sidebar`：左侧导航栏（固定定位）
- `.main-content`：主内容区域
- `.container`：内容容器（最大宽度 1200px）

**组件类：**
- `.contribution-card`：投稿卡片
- `.code-block`：代码块容器
- `.badge`：徽章标签
- `.like-button`：点赞按钮
- `.stat-item`：统计项（阅读量）

**状态类：**
- `.active`：激活状态（用于导航链接）
- `.liked`：已点赞状态
- `.open`：打开状态（移动端侧边栏）

## JavaScript 功能模块

### 核心函数

```javascript
// Markdown 解析相关
parseFrontmatter(content)        // 解析 YAML frontmatter
loadMarkdown(path)               // 加载 Markdown 文件

// 渲染函数
renderRadar(metadata, content)   // 渲染 Trending 部分
renderContribution(metadata, content)  // 渲染投稿卡片
loadContributions(vol)           // 加载所有投稿
loadArchive()                    // 加载归档列表

// 工具函数
getCurrentVol()                  // 获取当前期数（从 URL）
toggleSidebar()                  // 切换侧边栏（移动端）
toggleLike(button)               // 点赞功能

// 初始化
init()                           // 主初始化函数
```

### 数据流

1. **页面加载**：`DOMContentLoaded` → `init()`
2. **获取期数**：URL 参数 `?vol=XXX`（默认 001）
3. **并行加载**：
   - `loadArchive()` → 渲染侧边栏
   - `loadMarkdown(radar.md)` → `renderRadar()`
   - `loadContributions()` → `renderContribution()` × N

## 常见任务指南

### 添加新一期周刊

1. **创建目录结构**
```bash
mkdir -p content/vol-002/contributions
```

2. **复制并修改 radar.md**
```bash
cp content/vol-001/radar.md content/vol-002/radar.md
# 编辑 vol-002/radar.md，更新 frontmatter 和内容
```

3. **创建投稿文章**
```bash
# 在 content/vol-002/contributions/ 下创建 .md 文件
# 遵循命名规范：01-短标题.md, 02-短标题.md
```

4. **更新 archive.json**
```json
[
  {"vol": "002", "date": "2024.05.27", "active": true},
  {"vol": "001", "date": "2024.05.20", "active": false}
  // ...
]
```

5. **更新 index.html（如需要）**
```javascript
// 在 loadContributions() 函数中
const contributionFiles = [
  '01-new-article.md',
  '02-another-article.md',
  // ...
];
```

### 添加新的徽章类型

1. **在 CSS 中添加样式**
```css
.badge.newtype {
    background: rgba(R, G, B, 0.2);
    color: var(--accent-color);
}
```

2. **在 Markdown 中使用**
```markdown
### [新类型] 标题内容
```

### 修改配色方案

在 `index.html` 的 `:root` 中修改 CSS 变量：

```css
:root {
    --accent-cyan: #YOUR_COLOR;
    --accent-pink: #YOUR_COLOR;
    /* ... */
}
```

### 本地开发服务器

```bash
# Python
python3 -m http.server 8000

# Node.js
npx serve

# PHP
php -S localhost:8000
```

访问：`http://localhost:8000` 或 `http://localhost:8000?vol=001`

## 编码规范

### HTML
- 使用语义化标签（`<header>`, `<section>`, `<aside>`, `<footer>`）
- 注释清晰标记各个区块
- 响应式图片使用 `srcset`（如需）

### CSS
- 使用 CSS 变量管理颜色和间距
- BEM 命名规范（非强制，但推荐一致性）
- 移动优先的响应式设计
- 动画使用 `transition` 和 `animation`

### JavaScript
- 使用现代 ES6+ 语法
- async/await 处理异步操作
- 函数命名清晰表达意图
- 错误处理使用 try-catch
- 避免全局变量污染

### Markdown
- YAML frontmatter 使用两个 `---` 包裹
- 代码块指定语言标识符
- 链接使用相对路径（内部）或绝对路径（外部）

## 注意事项

### 重要约束

1. **跨域限制**：
   - 必须通过 HTTP 服务器访问，不能直接打开 `file://` 协议
   - 原因：`fetch()` API 不支持 `file://` 协议

2. **文件命名**：
   - 投稿文件建议使用数字前缀：`01-`, `02-`（便于排序）
   - 避免文件名包含空格和特殊字符
   - 使用小写字母和连字符

3. **Markdown 限制**：
   - Frontmatter 必须在文件最开头
   - YAML 语法严格（注意缩进）
   - 代码块的语言标识符区分大小写

4. **性能考虑**：
   - 每期投稿建议 2-6 篇
   - 避免过大的图片（建议 < 500KB）
   - 代码块行数建议 < 30 行

### 浏览器兼容性

- **推荐浏览器**：Chrome 90+, Firefox 88+, Safari 14+, Edge 90+
- **关键特性依赖**：
  - CSS Grid（主要布局）
  - CSS Custom Properties（CSS 变量）
  - ES6+ JavaScript（async/await, arrow functions）
  - Fetch API

### 已知问题

1. **Safari 中的 flexbox 问题**：已通过 `display: flex` 在 body 上解决
2. **移动端侧边栏滚动**：在 iOS Safari 可能有轻微卡顿
3. **代码高亮主题**：目前硬编码为 Tokyo Night Dark

## 扩展建议

### 可选功能
- [ ] 添加搜索功能（全文检索）
- [ ] 导出 PDF 功能
- [ ] RSS feed 生成
- [ ] 评论系统集成（Disqus/Giscus）
- [ ] 深色/浅色模式切换
- [ ] 国际化（i18n）支持

### 优化方向
- [ ] 图片懒加载
- [ ] Service Worker 离线支持
- [ ] 内容缓存策略
- [ ] 骨架屏加载状态
- [ ] 预加载下一期内容

## 文件修改历史

- `index.html`：动态 Markdown 加载版本
- `weekly-tech-radar.html`：静态参考模板（包含完整样式）
- 两个文件共享相同的 CSS 样式系统

## 开发工作流

1. **内容创作**：编辑 Markdown 文件
2. **本地预览**：启动 HTTP 服务器测试
3. **样式调整**：修改 CSS 变量或类
4. **功能开发**：修改 JavaScript 逻辑
5. **提交代码**：Git 提交并推送

## 联系与支持

如需协助或有任何疑问，请查看：
- `README.md`：用户使用指南
- Git 提交历史：了解功能演进
- 代码注释：详细的实现说明

---

**最后更新**：2026.01.26
**维护者**：Tech Radar Team
